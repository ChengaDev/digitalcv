{
  "name": "api",
  "version": "1.0.0",
  "description": "Fake api to mock real api calls",
  "main": "index.js",
  "scripts": {
    "start": "json-server -p 3001 -w db.json",
    "startWithFake": "json-server -p 3001 fakerData.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Chen Gazit",
  "license": "ISC",
  "dependencies": {
    "faker": "^4.1.0",
    "json-server": "^0.15.1"
  }
}